**Annotate when there is nothing to infer from.**

`let label: string` declares the type up front, before any value exists. Without the annotation its type would be `any`.

The colon syntax `name: Type` is the same everywhere: variables, parameters, return types, object fields.
