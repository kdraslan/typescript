**`T[]` is an array where every element is `T`.**

`number[]` holds any number of numbers. It has no fixed length, and every slot has the same type.

`Array<number>` is the exact same type, written the other way. Use whichever reads better; `T[]` is the common choice.
